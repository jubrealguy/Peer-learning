#include "search_algos.h"
#include <stdio.h>

int binary_search(int *array, size_t size, int value)
{
	int i, size = (int)size;
	int low = 0, high = end, mid = ;
	
	for (i = 0; i < siz; i++)
	{
		
	}
}
